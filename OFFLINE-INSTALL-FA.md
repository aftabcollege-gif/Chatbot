# رفع مشکل تکرار — نسخهٔ رایانهٔ بدون اینترنت

این راهنما برای حالتی است که:

- رایانهٔ دارای برنامه **به اینترنت وصل نیست** → پس هیچ لینک دانلودی روی آن کار نمی‌کند.
- PowerShell با **Run as administrator** و با **حساب ادمین شخص دیگری** باز شده است → پس مسیرهای پروفایل کاربر واقعی به‌صورت خودکار پیدا نمی‌شوند.

فایل `fix.ps1` برای همین دو شرط بازنویسی شده است.

---

## چرا نسخهٔ قبلی روی این رایانه کار نمی‌کرد

| مشکل | توضیح | راه‌حل در نسخهٔ جدید |
|---|---|---|
| دستور `Invoke-WebRequest` | رایانه اینترنت ندارد | فایل با **USB** یا **کپی/پیست** منتقل می‌شود |
| `$env:LOCALAPPDATA` | در حالت Run as administrator به پروفایل **ادمین** اشاره می‌کند، نه کاربر واقعی | همهٔ پروفایل‌های موجود در سیستم پیمایش می‌شوند |
| `$env:TEMP` | پوشهٔ موقت **ادمین** است | از پوشهٔ `hotfix-backup` داخل خود برنامه استفاده می‌شود |
| متن فارسی داخل اسکریپت | با Notepad یا ایمیل خراب می‌شد | کل فایل **ASCII خالص** است و فارسی به‌صورت base64 نگهداری می‌شود |

---

## روش ۱ — انتقال با USB (پیشنهادی)

۱. روی رایانه‌ای که **اینترنت دارد**، این فایل را باز کنید:

```
https://raw.githubusercontent.com/aftabcollege-gif/Chatbot/arena/01a09a02-chatbot/fix.ps1
```

۲. با `Ctrl+S` آن را با نام `fix.ps1` ذخیره کنید.

۳. فایل را در USB بگذارید و به رایانهٔ بدون اینترنت منتقل کنید — مثلاً در `C:\fix\`.

۴. چون فایل از رایانهٔ دیگری آمده، ویندوز آن را «مسدود» می‌کند. ابتدا این را اجرا کنید:

```powershell
Unblock-File C:\fix\fix.ps1
```

۵. سپس:

```powershell
cd C:\fix
powershell -ExecutionPolicy Bypass -File .\fix.ps1
```

---

## روش ۲ — کپی/پیست در Notepad

اگر USB در دسترس نیست:

۱. محتوای `fix.ps1` را از رایانهٔ متصل به اینترنت کپی کنید.
۲. روی رایانهٔ بدون اینترنت، **Notepad** را باز کنید و متن را پیست کنید.
۳. با `File → Save As` ذخیره کنید:
   - نام فایل: `"fix.ps1"` — **حتماً داخل گیومه**، وگرنه ویندوز آن را `fix.ps1.txt` می‌سازد.
   - مسیر: `C:\fix\`
   - Encoding: هر کدام باشد اشکالی ندارد (فایل ASCII خالص است).

> ⚠️ فایل عمداً بدون هیچ حرف فارسی نوشته شده تا با پیست‌کردن در Notepad خراب نشود.

---

## اگر پوشهٔ برنامه پیدا نشد

چون شما با حساب ادمین دیگری وارد شده‌اید، ممکن است اسکریپت نصب کاربر واقعی را نبیند. اول این را اجرا کنید:

```powershell
powershell -ExecutionPolicy Bypass -File .\fix.ps1 -List
```

این دستور هر نصبی را که می‌بیند فهرست می‌کند. اگر فهرست خالی بود، مسیر را دستی بدهید:

```powershell
powershell -ExecutionPolicy Bypass -File .\fix.ps1 -InstallDir "C:\Program Files\EnterpriseAI"
```

**پیدا کردن مسیر دقیق:** برنامه را باز کنید → **Task Manager** → روی نام برنامه راست‌کلیک → **Open file location**. پوشه‌ای که باز می‌شود همان مسیر است.

---

## نشانهٔ موفقیت

در خروجی باید این خط را ببینید:

```
Config files: 2
   backend\config\default.yaml   <- backend reads this
   config\default.yaml
```

عدد **۲** مهم است: نصب‌کننده دو نسخه از فایل تنظیمات می‌سازد و نسخه‌ای که واقعاً خوانده می‌شود همان `backend\config` است.

---

## بازگرداندن همه‌چیز به حالت اول

```powershell
powershell -ExecutionPolicy Bypass -File .\fix.ps1 -Revert
```

---

## آخرین راه‌حل — بدون هیچ فایلی

اگر انتقال فایل به هیچ شکلی ممکن نشد، این سه خط را مستقیم در PowerShell ادمین پیست کنید (مسیر را با مسیر واقعی جایگزین کنید). فقط تنظیمات را عوض می‌کند و موتور را دست نمی‌زند:

```powershell
$d="C:\Program Files\EnterpriseAI"
Get-ChildItem $d -Recurse -Filter default.yaml | Where-Object {$_.DirectoryName -match '\\config$'} | ForEach-Object {
  Copy-Item $_.FullName "$($_.FullName).bak" -Force
  $t=Get-Content $_.FullName -Raw -Encoding UTF8
  $t=$t -replace '(?m)^(\s+temperature\s*:).*','$1 0.3'
  $t=$t -replace '(?m)^(\s+max_tokens\s*:).*','$1 700'
  $t=$t -replace '(?m)^(\s+top_k\s*:).*','$1 2'
  $t=$t -replace '(?m)^(\s+chat_history_max_tokens\s*:).*','$1 200'
  [IO.File]::WriteAllText($_.FullName,$t,(New-Object Text.UTF8Encoding $false))
  Write-Host "patched $($_.FullName)" }
```

این دستور فقط همان چهار خط را عوض می‌کند و به `retrieval_top_k` و `reranker_top_k` دست نمی‌زند (به‌خاطر `\s+` ابتدای الگو). برای بازگرداندن، فایل‌های `.bak` را جایگزین کنید.

> این راه کوتاه **علت اصلی** را برطرف نمی‌کند و فقط تکرار را کمتر می‌کند. رفع کامل نیازمند اجرای `fix.ps1` است.

---

## نکتهٔ مهم دربارهٔ دسترسی فایل‌ها

چون فایل‌ها با حساب ادمین دیگری بازنویسی می‌شوند، اسکریپت عمداً:

- فایل‌های تنظیمات را **در جای خود** بازنویسی می‌کند (مالک و دسترسی‌ها حفظ می‌شود).
- دسترسی‌های فایل اجرایی اصلی را روی فایل جدید کپی می‌کند تا کاربر واقعی همچنان بتواند برنامه را اجرا کند.

پس از اجرا، یک‌بار با **حساب خود کاربر** برنامه را باز کنید تا مطمئن شوید بالا می‌آید.

---

## هشدار صادقانه

این اسکریپت روی ویندوز واقعی با مدل GGUF واقعی **آزمایش نشده است**. فقط منطق تغییر فایل تنظیمات، ساخت آرگومان‌های موتور، پیدا کردن مسیر نصب بین پروفایل‌ها، و قابلیت بازگشت روی لینوکس شبیه‌سازی و بررسی شده‌اند. اگر `csc.exe` روی سیستم نباشد، موتور دست‌نخورده می‌ماند و فقط تغییرات تنظیمات اعمال می‌شود — که وضعیت را بهتر می‌کند ولی تکرار را کاملاً از بین نمی‌برد.
